const AUTH_ROUTES = {
  login: "Login",
  register: "Register"
};

const MAIN_ROUTES = {
  main: "Main"
};

export const ROUTES = {
  auth: AUTH_ROUTES,
  main: MAIN_ROUTES
};

export const ROOT_ROUTES = {
  main: "Main",
  auth: "Auth",
  loading: "Loading"
};
